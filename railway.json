{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "cd backend && gunicorn medicheck.wsgi --bind 0.0.0.0:$PORT",
    "healthcheckPath": "/health"
  }
}